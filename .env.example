# --- DATABASE CONFIGURATION (for Postgres and PgAdmin) ---

# Hostname for Postgres. Use 'localhost' outside a Docker network.
DB_HOST=localhost 
# Port for the Postgres database
DB_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=supersecret
POSTGRES_DB=hospitals

# PgAdmin credentials
PGADMIN_DEFAULT_EMAIL=admin@hospitals.com
PGADMIN_DEFAULT_PASSWORD=supersecretadmin

# --- APPLICATION CONFIGURATION (for NestJS) ---

# NestJS listening port
PORT=3000

# Secret key for JWT signing
JWT_SECRET=myjwtsecret

# REDIS_HOST and REDIS_PORT are typically derived from your database service configuration.
# Assuming default ioredis settings and a locally running Redis instance:
# REDIS_HOST=localhost
# REDIS_PORT=6379